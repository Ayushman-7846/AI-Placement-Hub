/**
 * layouts/index.js — Layout Components Barrel Export
 *
 * Layout wrappers control the shell/chrome of different sections of the app.
 * Pages are rendered inside layouts, not standalone.
 *
 * Layout Architecture:
 *   AuthLayout      — Clean centered layout for Login/Register pages
 *   DashboardLayout — Full app layout with Navbar + Sidebar + main content
 *   PublicLayout    — Public-facing layout with marketing Navbar + Footer
 *
 *   import { DashboardLayout, AuthLayout } from '@layouts';
 *
 * Phase 1: Barrel setup — layouts implemented in Phase 2.
 */

// export { default as AuthLayout } from './AuthLayout';
// export { default as DashboardLayout } from './DashboardLayout';
// export { default as PublicLayout } from './PublicLayout';

export {}; // Phase 1 placeholder
