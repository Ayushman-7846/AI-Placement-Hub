/**
 * PostCSS Configuration
 *
 * NOTE: With Tailwind CSS v4 + the @tailwindcss/vite plugin,
 * PostCSS is NOT required for Tailwind processing.
 *
 * This file is kept for compatibility with other PostCSS tools
 * that may be added in future phases (e.g., postcss-import,
 * cssnano for production minification).
 *
 * If no additional PostCSS plugins are needed, this file can be removed.
 */
export default {
  plugins: {
    // Add PostCSS plugins here if needed in future phases
    // Example: 'cssnano': { preset: 'default' } for production minification
  },
};
