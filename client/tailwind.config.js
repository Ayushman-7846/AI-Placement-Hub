/**
 * Tailwind CSS v4 Configuration Note
 *
 * ⚠️  In Tailwind CSS v4, configuration has MOVED to CSS.
 *
 * There is NO tailwind.config.js in v4.
 *
 * All theme customization is done in src/index.css using the @theme directive:
 *
 * @theme {
 *   --color-primary: #6366f1;
 *   --font-sans: 'Inter', sans-serif;
 * }
 *
 * Content auto-detection is built-in — no content[] array needed.
 * Autoprefixer is built-in — no separate package needed.
 *
 * See: https://tailwindcss.com/docs/v4-beta#configuration
 * See: client/src/index.css for the actual theme configuration
 */

// This file is intentionally empty — Tailwind v4 uses CSS-first configuration.
// It is kept as a reference/placeholder to prevent confusion.
